auth:
  handler: function/Handler.start
  events:
    - http:
        path: /auth
        method: POST
        cors: true
register:
  handler: function/Handler.start
  events:
    - http:
        path: /user/register
        method: POST
        cors: true